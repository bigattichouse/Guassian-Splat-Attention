{
  "experiment_name": "splatflow_fixed_ultra_conservative_1749597556",
  "hardware_tier": "ultra_conservative",
  "dataset_config": "conservative",
  "hardware_config": {
    "name": "Ultra-Conservative 4GB - Emergency Fix",
    "description": "Emergency fix for memory crisis - 1K context",
    "memory_limit_gb": 3.5,
    "config": {
      "model_dim": 256,
      "num_layers": 3,
      "num_splats": 10,
      "max_splats": 128,
      "batch_size": 3,
      "gradient_accumulation_steps": 8,
      "mixed_precision": true,
      "gradient_checkpointing": true,
      "target_sequences": 256,
      "steps_per_epoch": 50,
      "seq_length": 512,
      "disable_adaptive_birth": false,
      "max_births_per_epoch": 2,
      "birth_cooldown": 5,
      "coverage_threshold": 0.03
    }
  },
  "training_config": {
    "model_dim": 256,
    "num_layers": 3,
    "num_splats": 10,
    "max_splats": 128,
    "max_seq_len": 2048,
    "dropout": 0.1,
    "epochs": 500,
    "batch_size": 3,
    "seq_length": 512,
    "target_sequences": 256,
    "learning_rate": 0.0003,
    "weight_decay": 0.01,
    "use_progressive_training": true,
    "warmup_epochs": 3,
    "eval_interval": 5,
    "eval_max_length": 50,
    "eval_temperature": 0.8,
    "eval_top_k": 50,
    "log_interval": 5,
    "save_interval": 10,
    "checkpoint_dir": "experiments/splatflow_fixed_ultra_conservative_1749597556/checkpoints",
    "dataset_name": "enhanced_real",
    "steps_per_epoch": 50,
    "gradient_accumulation_steps": 8,
    "mixed_precision": true,
    "gradient_checkpointing": true,
    "disable_adaptive_birth": false,
    "max_births_per_epoch": 2,
    "birth_cooldown": 5,
    "coverage_threshold": 0.03,
    "dataset_config": "conservative",
    "enable_detailed_monitoring": false
  },
  "created_at": "2025-06-10T18:19:16.047650",
  "context_length": 512,
  "splatflow_version": "1.0.0-fixed",
  "fixes_applied": [
    "Reduced model dimensions",
    "Conservative splat birth control",
    "Memory monitoring",
    "Gradient accumulation for small batches"
  ]
}